
apiVersion: v1
kind: Service

metadata:
  name: onlinebookstore-service

spec:
  type: NodePort

  selector:
    app: onlinebookstore

  ports:
    - port: 8080
      targetPort: 8081
      nodePort: 30080


